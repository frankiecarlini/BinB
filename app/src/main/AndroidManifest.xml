<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.frankie.binb">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:label="@string/app_name" android:name=".Paziente"/>
        <activity android:label="@string/app_name" android:name=".Audiogramma"/>
        <activity android:label="@string/app_name" android:name=".Paziente2"/>
        <activity android:label="@string/app_name" android:name=".Paziente3"/>
        <activity android:label="@string/app_name"  android:name=".Risultati"/>
        <activity android:label="@string/app_name" android:name=".BinauralB"/>


    </application>

</manifest>